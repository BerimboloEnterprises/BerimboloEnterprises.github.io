import React from 'react';
import { render } from 'react-dom';
import App from 'components/app';

// Renders the App.js data in the index.html tag.
render(<App />, document.getElementById('app'));